Tạo 30 mẫu cho mỗi `category` với `part = \"title\"` và `part = \"description\"` cho bài đăng bất động sản.

Các mẫu phải tuân thủ các quy tắc sau:
- `transaction_type` là \"cho thuê\".
- `category` lần lượt là \"nhà phố\", \"nhà mặt tiền\", \"căn hộ/ chung cư\", \"biệt thự\", \"đất nền\", \"kho/bãi\", \"mbkd\", \"khách sạn\", \"homestay\".
- `area` phải luôn đi cùng \"m²\"** (ví dụ: `<area>m²`).
- `structure`phải luôn đi cùng \"tầng\"
- `furniture` không được đi cùng cụm từ \"nội thất\"
- `function` đại diện cho công năng (số phòng ngủ, số wc, ...) của bất động sản nếu có
** \"mbkd\" đại diện cho mặt bằng kinh doanh **

Đối với `category = \"căn hộ/ chung cư\"`, `category = \"kho/bãi\",  `category = \"mbkd\"`, `category = \"đất nền\"`:
- **Không được chứa trường `<structure>`**.

Đối với `category = \"kho/bãi\",  `category = \"mbkd\"`, `category = \"đất nền\"`:
- **Không được chứa trường `<function>`, <furniture>**.

Đối với `transaction_type = \"cho thuê\"`:
- **Không được chứa trường `legal`**.

Đối với `part = \"title\"`:
- Chuỗi `value` phải **dưới 100 ký tự**.
- Chuỗi `value` phải bắt đầu bằng \"[`<transaction_type>` `<category>` giá `<price>` `<unit>`]\"
- **Không được chứa trường `description`, `pid`**.
- Trường `price` phải **luôn đi cùng trường `unit`** (ví dụ: `<price> <unit>`).
- **Thêm ngẫu nhiên placeholder \"<icon> \"**

Đối với `part = \"description\"`:
- Phải **luôn thêm \"☎ Liên hệ: 0375.155.525 - Đ.Bình\"** ở cuối chuỗi `value`.
- Phải **luôn có `pid`**, **luôn có trường `description` và đặt ở cuối cùng nhưng trên thông tin liên hệ**.
- Phải xuống dòng và **bắt đầu dòng mới bằng "<icon> "**.
- **Thêm ngẫu nhiên placeholder \"<icon> \"**
- Trường `pid` phải luôn đi kèm với "🆔: " ("🆔: <pid>")



Chỉ được sử dụng các trường sau làm placeholder (keyword) trong các chuỗi `value`:
`pid`, `transaction_type`, `province`, `district`, `ward`, `street`, `category`, `area`, `price`, `legal`, `structure`, `function`, `building_line`, `furniture`, `description`, `unit`, `icon`.



**Không thêm bất kỳ thông tin cụ thể nào được đại diện trực tiếp bởi các trường trên**
**Không thêm thông tin cụ thể về gần môt vị trí nào ngoại trừ "trường học", "chợ"**
**Yêu cầu kết quả là dữ liệu dạng .json. với mỗi item {
        "id": null,
        "is_default": 0,
        "transaction_type": \"transaction_type"\,
        "category": \"category"\,
        "part": "description", hoặc "part": "title",
        "value": \"transaction_type"\,
        "created_at": "thời gian hiện tại (2025-06-07 22:33:28.644642)",
        "updated_at": "thời gian hiện tại (2025-06-07 22:33:28.644642)",
    }**
**Yêu cầu output là format dạng json, xuất ra file để tôi có thể tải về**